# Coffee POS — Design System

ธีม: **"เครื่อง POS หน้าร้าน"** — จอสัมผัสที่พนักงานใช้ทั้งวัน สะอาด อ่านเร็ว กดไม่พลาด
พื้นขาว-เทาอ่อน ขอบบาง ตัวหนังสือเข้ม ปุ่มดำทึบ และมีสีส้ม `marigold` เป็นสีเน้นจุดเดียวสำหรับ action หลัก
ความ "สวย" ต้องมาจากระยะห่างและลำดับความสำคัญ ไม่ใช่จากสีหรือเงา

---

## 1. Color Tokens

| Token | Hex | ใช้ตรงไหน |
|---|---|---|
| `--ink` | `#1F1F1F` | ตัวหนังสือหลัก, ปุ่มดำ (ปุ่ม + บนการ์ดเมนู) |
| `--muted` | `#9A9A9A` | ตัวหนังสือรอง, placeholder, ชิปที่ยังไม่ active |
| `--line` | `#EDEDED` | เส้นขอบการ์ด, เส้นคั่นแถว, ขอบ input, ขอบ pill |
| `--surface` | `#F5F5F5` | พื้นบล็อกรูป, พื้น badge นับจำนวนที่ยังไม่ active |
| `--canvas` | `#F1F1F2` | พื้นหลังหน้าจอ (นอกการ์ด) |
| `--paper` | `#FFFFFF` | พื้นการ์ด, พื้นแถบหัว, พื้นชิป |
| `--amber` | `#F5C26B` | **action หลักเท่านั้น** (ยืนยันสั่ง) + badge ของชิปหมวดที่ active |
| `--coral` | `#F26B6B` | ปุ่มลบแบบทึบ, ปุ่มยืนยันที่เป็น destructive |
| `--matcha` | `#4F8B6E` | จุดบอกสถานะเปิดร้าน, ยอดขายเป็นบวก |
| `--cherry` | `#D1483B` | error message / toast |

```js
// tailwind.config.js — theme.extend.colors
ink: '#1F1F1F', muted: '#9A9A9A', line: '#EDEDED',
surface: '#F5F5F5', canvas: '#F1F1F2', paper: '#FFFFFF',
amber: '#F5C26B', coral: '#F26B6B',
matcha: '#4F8B6E', cherry: '#D1483B',
```

- ห้ามใช้สีนอกตารางนี้ ต้องการเฉดอ่อนให้ใช้ opacity ของสีเดิม (`bg-coral/10`)
- **`amber` ใช้ได้จุดเดียวต่อหน้าจอในฐานะปุ่ม** — ถ้ามีปุ่มอำพันสองปุ่มพร้อมกัน แปลว่าออกแบบลำดับความสำคัญผิด
  (badge ตัวเลขบนชิป active ไม่นับ เพราะไม่ใช่ปุ่ม action)
- `coral` กับ `cherry` ต่างกันที่หน้าที่: `coral` คือ**ปุ่ม**ที่กดแล้วลบ · `cherry` คือ**ข้อความ**บอกว่าเกิด error

---

## 2. Typography

ฟอนต์เดียวทั้งระบบ: **Plus Jakarta Sans** — ธีมนี้ไม่ใช้ฟอนต์ serif และไม่ใช้ mono

| Role | Class | ใช้ตรงไหน |
|---|---|---|
| หัวข้อหน้า/ส่วน | `text-lg font-bold text-ink` | "เมนู", "รายการสั่ง" |
| ราคา/ยอดรวม | `font-bold tabular-nums text-ink` | ราคาบนการ์ด, ยอดรวมตะกร้า (`text-2xl`) |
| ข้อความทั่วไป | ค่า default | ชื่อเมนู, label |
| ข้อความรอง | `text-sm text-muted` | คำอธิบาย, วันเวลา, สถานะว่าง |

**ทุกตัวเลขที่เรียงเป็นคอลัมน์หรือเปลี่ยนค่าได้ ต้องใส่ `tabular-nums`** (ราคา, จำนวน, นาฬิกา) ไม่งั้นความกว้างจะกระตุกตอนเลขเปลี่ยน

ราคาแสดงทศนิยม 2 ตำแหน่งเสมอ `55.00 ฿`

---

## 3. Signature Component — "Card"

```css
.card {
  background: var(--paper);
  border: 1px solid var(--line);
  border-radius: 12px;
}
```

- **ขอบบาง ไม่มีเงา** — ความลึกมาจากเส้นขอบกับพื้นหลัง `canvas` ไม่ใช่ `box-shadow`
- radius: การ์ด `rounded-xl` (12px) · ปุ่ม/ชิป/input `rounded-lg` (8px) · ปุ่มเล็กในแถว `rounded-md` (6px)
- เส้นคั่นภายในการ์ดใช้ `border-line` เส้นทึบ (ไม่ใช่เส้นปะแบบธีมเดิม)
- แถวสุดท้ายในลิสต์ตัดเส้นล่างด้วย `last:border-b-0`

**ปุ่ม**

| ระดับ | สไตล์ |
|---|---|
| Action หลัก | `bg-amber text-ink rounded-lg py-3 font-medium` |
| ปุ่มดำ (เพิ่มของลงตะกร้า) | `bg-ink text-paper rounded-lg` |
| ปุ่มรอง (เพิ่ม/ลดจำนวน) | `border border-line text-ink rounded-md` |
| ปุ่มลบ | `bg-coral text-paper rounded-lg` |
| ปุ่มข้อความ | `text-muted hover:text-ink` |
| Pill สถานะ/ข้อมูล (ไม่ใช่ปุ่ม) | `border border-line bg-paper rounded-lg px-3 py-1.5` |

**Toast** (แจ้งเตือนผลลัพธ์ เช่น "บันทึกแล้ว" / ข้อความ error)
- วางที่ **ขอบบนของจอ** `fixed top-20 left-1/2 -translate-x-1/2` — `top-20` เพื่อให้พ้นแถบหัว/navbar
- สำเร็จใช้ `bg-matcha` · ผิดพลาดใช้ `bg-cherry` · ตัวอักษร `text-paper` จัดกึ่งกลาง
- `pointer-events-none` ที่ตัวครอบ ไม่ให้บังการกดปุ่มที่อยู่ข้างใต้
- อยู่ 2.5 วินาทีแล้วหายเอง ไม่มีปุ่มปิด (ไม่ใช่ข้อมูลที่ต้องอ่านซ้ำ)

**ชิปหมวด** ไม่ใช้พื้นดำ — ตัวที่ active คือตัวที่ "สว่างกว่า" ไม่ใช่ "เข้มกว่า"
active: `bg-paper border-line font-semibold text-ink` + badge `bg-amber text-ink`
ปกติ: `bg-surface border-transparent text-muted` + badge `bg-paper text-muted`

ทุกชิปต้องมีพื้นหลังของตัวเอง (ไม่ปล่อยโปร่งใสบนพื้นหน้า) ไม่งั้นบนพื้น `canvas` จะแทบมองไม่เห็นขอบเขตปุ่ม
badge ของชิปที่ไม่ active ใช้ `bg-paper` เพื่อให้ตัดกับพื้นชิป `surface` — ห้ามใช้ `bg-surface` ซ้อน `bg-surface`

---

## 4. Layout ต่อหน้า

### Order (หน้าสั่งสินค้า — public, ไม่ต้อง login)
```
┌ แถบหัว (bg-paper, ขอบล่าง line) ────────────────────────────┐
│ [☕] Coffee POS              จันทร์ 31 ก.ค. 2569 · 09:48      │
│      ● เปิดให้บริการ                                          │
└──────────────────────────────────────────────────────────────┘
┌────────────────────────────────────────┬────────────────────┐
│ เมนู                      [🔍 ค้นหาเมนู] │ รายการสั่ง  ล้างทั้งหมด│
│ [ทั้งหมด 34][กาแฟสด 14][เย็น 12][ปั่น 8] │ ┌────────────────┐ │
│ ┌──────┐┌──────┐┌──────┐┌──────┐      │ │[รูป] ชื่อ    [🗑]│ │
│ │ รูป  ││      ││      ││      │      │ │ 110.00  − 2 + │ │
│ │ ชื่อ  ││      ││      ││      │      │ └────────────────┘ │
│ │55.00[+]│      ││      ││      │      │ ...                │
│ └──────┘└──────┘└──────┘└──────┘      │ ───────────────    │
│                                        │ ยอดรวม · 3 ชิ้น     │
│         2 / 3 / 4 คอลัมน์ตามจอ          │        165.00 ฿    │
│                                        │ [ ยืนยันสั่ง ]      │
└────────────────────────────────────────┴────────────────────┘
```
- **ชิปหมวดต้องมีตัวเลขกำกับเสมอ** — นับจากเมนูทั้งหมด **ไม่ผูกกับคำค้น** (ตัวเลขกระพริบตามที่พิมพ์จะอ่านไม่รู้เรื่อง)
- ชิปหมวดสร้างจากข้อมูลจริง ไม่ hardcode — หมวดที่ไม่มีเมนูจะไม่โผล่
- การ์ดเมนูมี **2 พื้นที่กดแยกกัน**: กดตัวการ์ด → popup เลือกจำนวน · กดปุ่ม **+** มุมขวาล่าง → เพิ่ม 1 ชิ้นทันทีไม่เปิด popup
  (ปุ่ม + เป็น sibling ของปุ่มการ์ด ไม่ใช่ปุ่มซ้อนปุ่ม — HTML ห้าม nested button)
- badge จำนวนที่อยู่ในตะกร้าเกาะมุมขวาบนของรูป เด้ง `animate-bounce` 1 รอบตอนเพิ่ม
- แถวตะกร้าต้องมีรูปย่อ (`size="sm"`) — บาริสต้าจำรูปได้เร็วกว่าอ่านชื่อ
- ตะกร้า sticky ด้านขวาบนจอ `lg` ขึ้นไป · ยอดรวมกับปุ่มยืนยันตรึงอยู่ท้ายการ์ดเสมอ

### Orders (บอร์ดครัว — admin เท่านั้น)
```
[card ORD-001] [card ORD-002] [card ORD-003]
   รายการ...       รายการ...      รายการ...
   [เสร็จแล้ว]      [เสร็จแล้ว]     [เสร็จแล้ว]

[card ORD-004] [card ORD-005]                  ↓ ตัดบรรทัดลงล่างเรื่อยๆ
```
- **grid ตัดบรรทัดลงล่าง ห้าม scroll แนวนอน** — มือถือ 1 / แท็บเล็ต 2 / จอคอม 3 ใบต่อแถว (`grid gap-4 sm:grid-cols-2 lg:grid-cols-3`)
  บาริสต้าต้องกวาดตาเห็นคิวทั้งหมดได้จากการเลื่อนแนวตั้งอย่างเดียว ไม่ต้องลากหาออเดอร์ที่ซ่อนอยู่ทางขวา
- การ์ดเป็น `flex flex-col` + ยอดรวมติด `mt-auto` → การ์ดในแถวเดียวกันสูงเท่ากัน ปุ่ม "เสร็จแล้ว" อยู่ระดับเดียวกันทุกใบ
- ออเดอร์ใหม่: `animate-card-in` เลื่อนขึ้นจากด้านล่าง 350ms
- กด "เสร็จแล้ว" → ปั๊มตรา ✓ สี `matcha` กลางการ์ด แล้ว `animate-card-out` ใน 400ms
- ปุ่ม "เสร็จแล้ว" ใช้ `bg-matcha` ไม่ใช่ `amber` — เป็น action ที่มีความหมายว่า "สำเร็จ" ตามตาราง §1

### Dashboard (ยอดขาย — admin เท่านั้น)

แบ่ง 2 โซนชัดเจน: **ภาพรวมปัจจุบัน** (บนสุด ไม่ผูกกับตัวเลือกใดๆ) แล้วค่อย **เจาะดูตามช่วงเวลา**

```
┌──────────┐ ┌──────────┐ ┌──────────┐
│ วันนี้    │ │ เดือนนี้  │ │ ปีนี้     │   ← กดได้ (กระโดดไปดูช่วงนั้น)
│ ฿ 4,250  │ │ ฿ 82,100 │ │ ฿ 940,300│   ← count-up animation ตอนโหลด
│ ▬▬▬      │ │          │ │          │   ← ขีด marigold ใต้การ์ดที่ active
└──────────┘ └──────────┘ └──────────┘

เจาะดูตามช่วงเวลา
[pill tabs: รายวัน|รายเดือน|รายปี]  [ตัวเลือกวัน/เดือน/ปี]

  ยอดขาย · 31 กรกฎาคม 2569
  4,250.00 ฿
  ▲ 12.4% จากเมื่อวาน (3,780.00 ฿)      ← matcha ขึ้น / cherry ลง
  ─────────────────────────────
  จำนวนออเดอร์ 18    เฉลี่ยต่อออเดอร์ 236.11 ฿

ขายอะไรไปบ้าง
  ลาเต้เย็น          ×24   1,320.00 ฿
  ▬▬▬▬▬▬▬▬▬▬▬▬▬▬          ← แถบเทียบสัดส่วนกับเมนูอันดับ 1
  อเมริกาโน่         ×15     750.00 ฿
  ▬▬▬▬▬▬▬

รายการออเดอร์ (แถวแนวตั้ง แบบ list ใบเสร็จ)
```
- **ภาพรวม 3 การ์ดต้องเห็นพร้อมกันเสมอ** ห้ามยุบเป็น switcher ที่ดูได้ทีละช่วง — จุดประสงค์คือเทียบด้วยตาในครั้งเดียว
- ตัวเลขยอดขาย: นับขึ้นจาก 0 ด้วย ease-out ~800ms ตอน mount (`requestAnimationFrame`, ไม่ใช้ library)
- **เทียบช่วงก่อนหน้าเสมอ** — ตัวเลขเปล่าๆ บอกไม่ได้ว่าดีหรือแย่ ขึ้นใช้ `matcha` ลงใช้ `cherry` พร้อมยอดของช่วงก่อนในวงเล็บ
  ถ้าช่วงก่อนหน้ายอดเป็น 0 **ห้ามโชว์ %** (หารศูนย์) ให้ขึ้น "ช่วงก่อนหน้าไม่มียอดขายให้เทียบ" แทน
- แถบสัดส่วนรายเมนู `h-1` พื้น `bg-surface` แท่ง `bg-amber` — **ไม่มี animation** (ไม่อยู่ในตาราง §5)
- รายการยาวแสดง 10 เมนู / 20 ออเดอร์แรก แล้วต่อด้วยปุ่ม "ดูทั้งหมด N รายการ" (ดู §6)

---

## 5. Motion Guidelines

| Interaction | Animation |
|---|---|
| กดปุ่ม/ปุ่มเล็ก | `active:scale-90 transition-transform duration-100` |
| กดการ์ดเมนู | `active:scale-[0.98] transition-transform duration-100` |
| กดปุ่มหลัก (ยืนยันสั่ง) | `active:scale-95 transition-transform duration-100` |
| เพิ่มของลงตะกร้า | badge `animate-bounce` 1 รอบ (~400ms) |
| ออเดอร์ใหม่เข้าบอร์ด | `animate-card-in` — slide-up 16px + fade ~350ms ease-out |
| ยืนยันออเดอร์เสร็จ | `animate-stamp` scale-in (0 → 1.1 → 1) 250ms แล้ว `animate-card-out` 400ms |
| ตัวเลขยอดขาย dashboard | count-up 800ms ease-out ตอน mount ครั้งเดียว |
| toast โผล่ | `animate-toast-in` — เลื่อนลง 8px + fade 200ms ease-out |
| เปลี่ยนสีตอน hover | `transition-colors` |

**กฎสำคัญ:** `prefers-reduced-motion: reduce` ตัด animation ทั้งหมด (ทำไว้แล้วใน `index.css` แบบ global)
ห้ามใส่ animation นอกตารางนี้ — ความสม่ำเสมอสำคัญกว่าลูกเล่นเยอะ

---

## 6. สิ่งที่ห้ามทำ

- **ห้าม scroll ซ้อน scroll** — ห้ามใส่กล่อง `max-h-* overflow-y-auto` ในหน้าที่เลื่อนได้อยู่แล้ว (ล้อเมาส์แล้วเดาไม่ออกว่าอันไหนจะเลื่อน)
  รายการยาวให้ตัดแสดงบางส่วนแล้วต่อท้ายด้วย `<ShowMore>` — ได้เรื่อง performance ด้วยเพราะไม่ต้อง render ทั้งก้อน
  **ข้อยกเว้นเดียว:** พาเนลข้างที่ sticky และสูงตาม viewport (ตะกร้าหน้า Order) เลื่อนภายในตัวเองได้ เพราะไม่ได้เลื่อนไปพร้อมหน้า
- **ปุ่มขยายรายการต้องย่อกลับได้เสมอ** — ใช้ `components/ShowMore.jsx` ตัวเดียวทุกที่ สลับข้อความระหว่าง
  "ดูทั้งหมด N รายการ" กับ "ดูน้อยลง (เหลือ N รายการ)" ห้ามทำปุ่มที่กดขยายแล้วย่อกลับไม่ได้
  component ซ่อนตัวเองอัตโนมัติเมื่อจำนวนยังไม่เกิน limit จึงเรียกใช้ได้เลยโดยไม่ต้องเช็คเงื่อนไขซ้ำที่หน้าเรียก
- **ห้าม scroll แนวนอนกับรายการหลักของหน้า** — ของที่หลุดออกนอกจอทางขวามักไม่ถูกมองเห็นเลย ใช้ grid ตัดบรรทัดลงล่างแทน
- ห้ามใส่เงาให้การ์ด (`shadow-*`) — ธีมนี้ใช้เส้นขอบสร้างความลึก
- ห้ามใส่ gradient พื้นหลังลอยๆ ที่ไม่มีความหมาย
- ห้ามใช้ `amber` เป็นปุ่มเกินหนึ่งจุดต่อหน้าจอ
- ห้ามใช้ปุ่มซ้อนปุ่ม (`<button>` ใน `<button>`) — HTML ไม่ถูกต้องและกดพลาดง่าย ใช้ sibling + `absolute` แทน
- เลข 01/02/03 หรือ numbered marker ใช้เฉพาะตอนมีลำดับจริง ไม่ใช่ตกแต่งเฉยๆ

---

## 7. รูปเมนู + Fallback

**รูปจริง:** backend crop เป็นจัตุรัส + resize 600×600 (webp) ตั้งแต่ตอนอัปโหลด
→ ฝั่ง UI แสดงในกรอบ **อัตราส่วน 1:1 เสมอ** (`aspect-square object-cover rounded-lg`)

**Fallback เมื่อไม่มีรูป** (`image_url` = null): บล็อกพื้น `bg-surface` + emoji ตามหมวดกลางบล็อก (ไม่โชว์ไอคอนรูปเสีย)

| หมวด | Emoji |
|---|---|
| กาแฟสด | ☕ |
| เย็น | 🧊 |
| ปั่น | 🥤 |
| แอลกอฮอล์ | 🍺 |
| อื่นๆ | ✨ |

- ธีมใหม่ใช้พื้นเทากลาง `surface` เหมือนกันทุกหมวด — สีประจำหมวดถูกยกเลิก เพราะพาเลตต์ใหม่เก็บสีไว้ให้ status เท่านั้น
- `size="lg"` (default) emoji `text-5xl` สำหรับการ์ด/popup · `size="sm"` emoji `text-xl` สำหรับรูปย่อในตะกร้า
- ใน **Menu Manage** ช่องอัปโหลดที่ยังไม่มีรูปให้แสดง fallback เดียวกัน + ปุ่ม "อัปโหลดรูป" ทับมุม

---

## 8. Navbar (admin)

```
Coffee POS   เมนู  ออเดอร์ ③  จัดการเมนู  ยอดขาย                        [↪]
──────────────────────────────────────────────────────────────────────────
```
- `bg-paper` + `border-b border-line` — ไม่ใช่แถบสีทึบ
- ลิงก์ที่ active: `bg-surface font-semibold text-ink` · ปกติ: `text-muted hover:text-ink`
- badge จำนวนออเดอร์ค้าง: `bg-coral text-paper` วงกลม
- ออกจากระบบเป็น**ไอคอนอย่างเดียว** (`text-muted` → hover เป็น `coral`) ต้องมี `aria-label` + `title` เสมอ
- **ไม่แสดงชื่อผู้ใช้และไม่มีโลโก้** — ร้านมี admin ไม่กี่คนและใช้เครื่องเดียวกัน ชื่อคนล็อกอินไม่ใช่ข้อมูลที่ต้องดูตลอดเวลา
  พื้นที่บนแถบควรเหลือไว้ให้ลิงก์กับ badge ออเดอร์ค้างซึ่งต้องเห็นตลอด
- จอเล็กกว่า `md` ยุบเป็นแฮมเบอร์เกอร์ พร้อม badge รวมเกาะมุมปุ่ม

---

## หมายเหตุประวัติ

ก่อนหน้านี้โปรเจกต์ใช้ธีม "ทุกอย่างคือใบเสร็จ" (พื้นครีม `#FBF3E9` + น้ำตาล `#4A2E2A`, การ์ดขอบปะ `.ticket`,
ฟอนต์ Fraunces + Space Mono) — **ถูกยกเลิกทั้งหมดแล้ว** ทั้ง token, คลาส `.ticket` และฟอนต์ทั้งสองถูกลบออกจาก
`tailwind.config.js` / `index.css` / `index.html` ถ้าเจอ `grounds` `foam` `marigold` `ticket` `font-display`
`font-mono` ที่ไหนในโค้ด แปลว่าเป็นของตกค้าง ให้แก้เป็น token ใน §1
